# Лекторий → ролики Фреди (стиль Мистера Фримена)

Адаптация движка `animdsl` под видео к лекциям Лектория. К каждой лекции
делается короткий **чёрно-белый рисованный ролик**, где персонаж **Фреди**
говорит в камеру — в манере Мистера Фримена: резкий контраст, крупные планы,
минимум фона.

Видео **немое**. Закадровый голос берётся из серверного TTS лекции (репозиторий
`Dron939-sketch/Frederick`, `backend/blog_tts_routes.py`) и подмешивается на
этапе сборки. Это разделение — движок рисует, TTS озвучивает — и есть суть
адаптации.

## Конвейер

```
лекция (blog/lekciya-*.html)          TTS лекции (Frederick, .mp3)
        │                                     │
        ▼                                     │
tools/gen_lecture_anim.py                     │
        │  .anim (немой сценарий Фреди)        │
        ▼                                     │
animdsl render → result.mp4 (ч/б, без звука)  │
        │                                     │
        └──────────► tools/compose_video.sh ◄─┘
                            │
                            ▼
                   финальный ролик (видео + голос + титр)
```

## Персонаж Фреди

Основной Фреди — **детальный SVG-риг** `examples/assets/characters/fredi_rig/`:
лысый, тонкие круглые очки, чёрный смокинг с атласными лацканами, белая сорочка,
бабочка, платок-паше, трость. Части нарисованы вектором с градиентной
растушёвкой и рендерятся через resvg — гладко, со сглаживанием и объёмом.
Образ — Стэтхем × Воланд: угловатая челюсть, тяжёлый взгляд.

**Артикуляция** (иерархический скелет, FK — как в Spine/DragonBones):
- тело: `torso → head`, руки `upper_arm → forearm` (локоть), ноги
  `thigh → shin` (колено) — жесты сгибаются в суставах, а не «трубками»;
- **мимика**: подвижные `eye_left/right` (моргание), `brow_left/right`
  (хмурятся/поднимаются), `mouth` (открывается для речи).

**Библиотеки** в `rig.json`:
- выражения/мимика: `fredi-calm, blink, speak, stern, angry, surprised,
  skeptical, smirk, think, ...`;
- жесты/движения: `explain, point, lean` (со сгибом локтя/колена).

Витрина мимики — `fredi-expressions-demo.anim`.

Есть и запасной **процедурный** Фреди (`assets/characters/procedural/fredi.json`)
— математический рендер (лысина+блик, круглые очки, бабочка, смокинг с фалдами,
трость) для случаев, когда нужен вариант без SVG. Под него в движок добавлены
`HairStyle::Bald`, `AccessoryKind::BowTie`/`Cane`, `ClothingKind::Tailcoat`,
круглые/тонированные очки и пропорции (`head_scale`, `leg_scale`, …).

Общие приёмы стиля:

- `config { monochrome: true }` — мягкая постобработка в ч/б («тушь»), сохраняет
  градиентную растушёвку;
- направление `facing front` — Фреди смотрит прямо в камеру;
- светлый фон-«бумага» `lektorij-paper.svg`.

## Библиотека поз

Канон — в `fredi-lecture-template.anim` и в генераторе (`POSE_LIBRARY`). Все позы
фронтальные, лицом в камеру:

| Поза            | Смысл                                   |
|-----------------|-----------------------------------------|
| `fredi-calm`    | база, спокойное наблюдение              |
| `fredi-blink`   | моргание, микропауза                    |
| `fredi-think`   | задумчивость, рука к подбородку         |
| `fredi-explain` | открытый жест, объяснение               |
| `fredi-point`   | указание, «это про тебя»                |
| `fredi-stern`   | строгий акцент                          |
| `fredi-smirk`   | усмешка, «я знаю, к чему веду»          |
| `fredi-lean`    | наклон вперёд, усиление                 |

Меняете позы — правьте оба места синхронно (шаблон и генератор).

## Как сделать ролик к лекции

1. **Сгенерировать сценарий** из HTML лекции:

   ```
   python3 tools/gen_lecture_anim.py \
       ../dron939-sketch.github.io/blog/lekciya-2-frejd-psihodinamika.html
   ```

   Появится `examples/lektorij/lekciya-2-frejd-psihodinamika.anim`
   (заголовок → интро, каждый раздел «N. …» → доля, «Итоги» → финал).

2. **Отредактировать** реплики `VO:` в комментариях и подогнать паузы `wait`
   под длительность озвучки. Стиль — по методичке лекций
   (`blog/lektorij/METODICHKA.md` на сайте): цель — понимание, не пересказ.

3. **Проверить и отрендерить**:

   ```
   cargo run --release -- check  examples/lektorij/lekciya-2-frejd-psihodinamika.anim
   cargo run --release -- render examples/lektorij/lekciya-2-frejd-psihodinamika.anim -o fredi.mp4
   ```

4. **Собрать со звуком** (и титром):

   ```
   tools/compose_video.sh fredi.mp4 lekciya-2.mp3 lekciya-2-final.mp4 "Лекция 2. Фрейд"
   ```

## Пилот

`lekciya-2-frejd-psihodinamika.anim` — эталонный, вручную отшлифованный ролик
(лекция «Фрейд: психодинамическое направление» курса «Теории личности»).
Именно он рендерится по умолчанию в `Dockerfile` и в CI (`.github/workflows/render.yml`).

## Массовая генерация

Лекций ~720 в ~72 курсах. Прогнать генератор по всем:

```
for f in ../dron939-sketch.github.io/blog/lekciya-*.html; do
    python3 tools/gen_lecture_anim.py "$f"
done
```

Затем рендерить нужные и собирать со звуком. Тайминг у автогенерации
эвристический — под финальный монтаж паузы правятся под реальную озвучку.

## Живость кадра: замер по всем роликам, 09.09.2026

Приёмщик `studio.lint_zhivost` считает долю времени, когда в кадре меняется
меньше 2% площади, то есть двигается один рот. Первый же прогон по всему
репозиторию дал картину, которую стоит держать перед глазами: **это не беда
одного ролика, это привычка снимать говорящую голову перед задником.**

| ролик | «только рот» | самый долгий покой |
|---|---|---|
| myshlenie-intro | 62% | 3.8с с 0.4с |
| teorii-lichnosti-intro | 50% | 2.8с с 53.0с |
| snova-zhivoj-intro | 46% | 3.2с с 31.1с |
| prison-intro | 42% | 3.2с с 69.8с |
| filosofiya-intro | 42% | 2.1с с 5.6с |
| svoe-delo-intro | 41% | — |
| istoriya-idej-intro | 40% | — |
| samoocenka-intro | 40% | — |
| pereproshivka-intro | 40% | 3.5с с 0.0с |
| **gnev-intro** (после правки) | **37%** | 1.2с с 51.2с |

Цель — не больше 35% и не больше 2.0с подряд. Ни один ролик её пока не берёт;
«Гнев» ближе всех, и взят он не камерой, а тем, что в кадре начали
происходить события: ребёнок уходит, камень срывается, дверь закрывается,
капля падает. Камера при этом почти не изменилась — а вот у трёх роликов из
списка она НЕ ДВИНУЛАСЬ НИ РАЗУ за минуту, и это отдельное замечание того же
приёмщика.

Что с этим делать, по убыванию отдачи: событие в кадре (предмет играет
реплику) → вход и уход персонажа шагом → наезд на ударном слове → кат.
Последний пункт самый дешёвый и самый бесполезный: кат переключает внимание,
но не держит его.
