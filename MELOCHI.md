# «Одна голова»: мелочи, которые решают

Дистанция с оригиналом сократилась (ритм, ракурсы, мимика, типографика).
Остаток — не крупные приёмы, а **мелочи фактуры**: то, из-за чего кадр Фримена
выглядит снятым, а наш — сгенерированным. Их не замечают поштучно, но их
отсутствие чувствуют все.

## Ревизия: что уже есть

| Мелочь | Статус |
|---|---|
| Моргание (авто, ~раз в 3.4–4.6с, снап) | ✅ `skeleton` auto-blink |
| Дыхание/покачивание в покое | ✅ multi-frequency breath |
| Дрейф позы (некогерентная статика) | ✅ smooth coherent drift |
| Дрожь линии (боил) | ✅ `line-boil` |
| Зерно плёнки, виньетка | ✅ `film-grain`, `vignette` |
| Частицы (снег/пепел) | ✅ `snow` |
| Смазы на быстрых движениях | ✅ по угловой скорости |
| Контакт-тень, свет+тень+кромка | ✅ ground/cast/form/rim |

## Чего нет — и что даёт максимум на единицу усилий

**Группа А. Плёнка живая, а не цифровая** (кадр «дышит» сам по себе):
1. **Мерцание экспозиции** (`film-flicker`) — яркость кадра плавает на 2–5%
   покадрово. Главный признак плёнки; без него картинка «мертво ровная».
2. **Гуляние кадра в тракте** (`gate-weave`) — весь кадр микро-смещается на
   0.5–2px по X/Y с низкой частотой. Плёнку ведёт в лентопротяжном механизме.
3. **Царапины** (`film-scratch`) — тонкие вертикальные линии, живут 3–15 кадров
   и пропадают. Появляются редко, читаются подсознательно.
4. **Пылинки/ворс** (`film-dust`) — 1–5 тёмных точек/волосков на кадр, каждый
   кадр новые. Классика 16-мм.

**Группа Б. Живость подачи** (мелочи актёрской игры):
5. **Задержка реакции** — герой реагирует на своё же слово на 2–3 кадра позже,
   не синхронно. Синхронность = кукла.
6. **Асимметрия покоя** — плечи/наклон маски никогда не идеально ровные.

**Группа В. Звук** (мелочи, которых не слышно, но без них «пусто»):
7. **Дыхание между репликами** — вдох перед ударной фразой.
8. **Комнатный тон** — тихий эмбиенс вместо стерильной цифровой тишины.

## Порядок реализации

1. **Группа А целиком** — четыре пост-эффекта кадра: дёшево по перфу
   (пиксельные операции поверх готового кадра), детерминированно (хэш от номера
   кадра — golden-diff остаётся воспроизводимым), opt-in по конфигу.
2. Группа Б — в скелете/таймлайне.
3. Группа В — в звуковом цехе (`sfx.py`, `voiceover.py`).

Каждая мелочь — ключ конфига, по умолчанию 0 (старые сцены байт-в-байт),
и включается в пресете «Фримен» для новых роликов.

---

## Замер дёрганья (жёсткий анализ, по запросу студии)

Метрика: средняя покадровая разница яркости (mean abs diff), участок речи
крупным планом, 320×180. 0 = мёртвая статика; >4 = заметное дёрганье.

| Конфигурация | Дёрганье |
|---|---|
| исходная (`film-grain 0.30`, boil 0.6, on-twos 2) | **4.39** |
| boil 0.0 (боил выключен) | 4.33 |
| boil 0.25 | 4.33 |
| on-twos 1 (гладкие 24fps) | 4.92 *(хуже!)* |
| **film-grain 0.15** (боил и двойки не тронуты) | **2.02** |
| film-grain 0.0 | 1.98 |

**Вывод:** дёрганье давал НЕ боил и НЕ анимация на двойках, а **зерно**.
Оно пересчитывалось каждый кадр и шипело на 24 fps поверх рисунка, который
живёт на 12 — глаз читал это как дрожь персонажа. Профиль подтверждал:
`30, 1, 30, 1, 25, 1…` — прыжок/дубль.

**Исправлено двумя ходами:**
1. Зерно ДЕРЖИТСЯ вместе с рисунком (хэш от `frame / on_twos`) — шум больше не
   живёт своей жизнью: 4.39 → 3.44 при той же амплитуде.
2. Амплитуда в роликах снижена 0.30 → 0.16 (фактура сохраняется, шипение
   уходит): итог **2.04 — дёрганье вдвое меньше исходного**.

Голдены эталона переутверждены (изменение картинки намеренное).
