# Аудит движка и дорожная карта до «рисованного голливудского фильма»

Полная ревизия движка (рендер, камера, риг/анимация, DSL/локации/завод) с
целью: на выходе — рисованный фильм уровня Голливуда, максимум внимания к
мелочам, локациям, сценарию, монологам, картинке. Сгруппировано по
подсистемам, отмечено СДЕЛАНО в этом проходе и приоритет остального.

Стиль-эталон — анимация «Мистер Фримен»: ч/б тушь, boil, on-twos, резкий
монтаж, агрессивный форшортенинг, прямое обращение к зрителю.

---

## 0a. Спринт 1 — покадровость и надёжность (по ревизии заказчика)

- **A1 Единый временной шаг — СДЕЛАНО.** Один клок: `t`(=anim_t) + `step_dt`
  ведут позу, ink-boil и smear-prev. Линия «перерисовывается» РОВНО на снапе
  позы (раньше boil полз на t*5 вне такта on-twos). Замерено: внутри удержания
  кадр персонажа пиксель-идентичен (0.000); зерно — по кадрам (плёнка).
- **A2 Фримен-пресет по умолчанию — СДЕЛАНО.** `on_twos=2`, `line_boil=0.6`,
  `film_grain=0.08` в `RenderConfig::default` — ролик не стартует «стерильным».
- **Спокойный idle/речь + смазы по порогу — СДЕЛАНО.** Idle/речь-микродвижения
  урезаны ~в 2.5×, порог смаза нормирован по высоте кадра (не плодит призраков
  на мелком дрожании) → меньше «дёрганья» при ×N персонажах.
- **B1 QC-гейт — СДЕЛАНО.** `studio.py --strict` (в CI): падаем, если у
  продакшена заявлен `vo`, а в финале нет звука, или рассинхрон video/final >20%.
- **B3 CI рендерит ВСЕ продакшены — СДЕЛАНО** (не один захардкоженный).
- Осталось (Спринт 2): overlapping action (per-bone задержка), смазы по угловой
  скорости, свести полезное из `procedural/` в рабочий путь. (Спринт 3) hero-cels.
  (Завод) артефакты вне git/LFS, кэш парсинга SVG, схема VO + линтер регистра.

## 0c. Спринт 2–3 (движение и графика)

- **A3 Overlapping action — СДЕЛАНО.** Пружинный лаг расширен на кисти/ноги/
  шляпу/трость: жест уходит по цепи (торс → плечо → предплечье → кисть), а не
  снапится разом (главный «кукольный» тэлл).
- **A4 Смазы по угловой скорости — СДЕЛАНО.** Быстрый мах вокруг сустава мажется
  по инерции (вес рисунка), призрак всегда ЗА живой частью.
- **A5 Форм-тень (cel-объём) — СДЕЛАНО.** `form-shadow: <сила>` + `light-angle`:
  сторона против света темнеет жёсткой cel-гранью (SourceAtop, клип по силуэту)
  — маска получает объём. Вместе с cast-shadow = свет+тень «в одном свете».
- **A6 Hero-cels — уже есть** (8 рисованных лиц: rage/scream/shock/dead/
  skeptic/sly/wow/laugh; 13 поз hero_/flash_ свапают всю голову). Механизм готов;
  добор новых лиц — по мере съёмок.
- **A7 Rim/контровой — СДЕЛАНО.** `rim-light: <сила>` + `light-angle`: тёплая
  офф-вайт кромка на освещённой стороне силуэта (тонированный силуэт со сдвигом
  к свету, рисуется ЗА фигурой — торчит только освещённая грань). Отделяет фигуру
  от тёмного фона; парой к form-shadow даёт полный объём «свет+тень+кромка».
- Осталось: сведение cloth-пружины из `procedural/` (подол/фалды), свет как
  реплика сцены (переключение key/fill/rim по ходу), rim-color/rim-angle
  отдельно от key, завод: артефакты вне git, кэш парсинга, VO-линтер.

## 0b. Свет и тень (первый проход)

- **Отбрасываемая тень-силуэт** (`cast-shadow: <сила>`, `light-angle: <deg>`):
  персонаж рендерится в отдельный слой, из него берётся чёрный силуэт и
  проецируется на пол (сдвиг+сплющивание по направлению света), под фигурой —
  киношный объём. Off по умолчанию. Применено в интро «Бизнес-мышление».
- Осталось (приоритет №1 картинки): form-shadow/cel-тень на маске (светлая
  часть) по вектору света + контровой (rim) + drop-shadow на стену.

## 0. Сделано в этом проходе (Рубеж «камера-объектив + картинка»)

- **Камера-объектив: вертикальный ракурс + настоящий форшортенинг.**
  Добавлены `camera pitch <deg>` и сахар `camera angle high|low|level [target]`.
  Наклон объектива держится поперёк склеек. В рендере — `apply_camera_pitch`:
  ближняя к камере часть тела крупнее, дальняя — мельче и сжата к линии схода
  (чистая функция экранного Y, стыки суставов сохраняются). Теперь пропорции
  и углы считаются «из объектива», а не подгоняются вручную. Пресет-позы
  `low_angle`/`high_angle` стыкуются с наклоном для объёмного прочтения.
- **Билинейная фильтрация** на композите частей (было nearest → лесенка и
  дрожание тушевой линии по кадрам), бикубик на фоне/сете, билинейка на dutch.
- **Контактная тень под ногами** (`ground-shadow: 1`, off по умолчанию):
  мягкая тень по истинному низу силуэта (по height части) — фигура «стоит»,
  не парит. Для не-тушевых/мягких сцен; в жёстком ч/б включать дозированно.

---

## 1. Рендер / картинка (src/renderer, src/main)

Приоритет: HIGH = сильно бьёт по «киношности».

- [СДЕЛАНО] Форшортенинг из камеры (см. §0). Осталось развить: настоящий
  per-bone `z_depth` (жест/кулак В УПОР к камере крупнее тела — сейчас только
  вертикальный ракурс; глубина по оси Z ещё не моделируется).
- [СДЕЛАНО] Билинейная/бикубическая фильтрация композита.
- [HIGH] **Свет и тень**: нет cel-shading, form-shadow, rim-light, ambient
  occlusion. Голливудское 2D всегда имеет ключ/контровой/форму. План: пасс
  cel-тени (multiply-копия части со сдвигом по вектору света) + rim по светлой
  кромке; глобальный вектор света в конфиге/сцене.
- [СДЕЛАНО частично] Контактная тень (§0). Осталось: тень, повторяющая позу
  (drop-shadow силуэта на пол/стену), а не только эллипс под ногами.
- [HIGH] **Rubber-hose шов**: `draw_bent_pixmap` режет часть на 2px-полосы,
  каждая под одним углом → тёмный шов от двойного альфа-блендинга на выпуклой
  стороне и фасетки на сильном изгибе. План: посэмплово билинейно по строкам
  (непрерывный угол), композитить полосы по max-покрытию, не alpha-over.
- [MED] **Смиры движения** — сейчас это полупрозрачные КОПИИ части (строб),
  порог `motion>16` в сырых пикселях (зависит от разрешения), и призрак
  наследует z_order (рука-призрак перед торсом). План: нормировать порог по
  высоте кадра; рисовать вытянутый по вектору смир/направленный блюр; призрак
  строго ЗА живой частью.
- [MED] **Единый временной шаг**: pose_time (12fps), boil-seed (t*5), smear
  (1/12), on-twos (config) — четыре разных клока; boil «ползёт» внутри
  held-кадра, размывая on-twos. План: один `drawing_step = floor(t*fps/on_twos)`
  и от него всё.
- [MED] **Переходы**: cut/dissolve/wipe по факту все «в чёрное» — нет второго
  буфера сцены. План: рендерить входящую сцену во второй pixmap и
  кросс-композитить (dissolve=alpha A→B, wipe=раскрытие B, cut=hard swap).
- [MED] **Boil растра** (`apply_line_boil`) — nearest-копия с округлением до
  целого пикселя → рвёт AA тушевой кромки. План: билинейная выборка или boil
  вектора (seed feTurbulence), без растрового.
- [MED] **Глубина/атмосфера**: фон в полную резкость, без параллакса и
  воздушной перспективы. План: блюр+десатурация дальнего плана по «depth»,
  параллакс-фактор <1 на camera.x/y.
- [LOW] Dutch: overscale фикс `1+roll*0.012+0.02` может не покрыть углы —
  считать по `|sin|+|cos|`. (Билинейку уже добавили.)
- [LOW] Грейд: одна S-кривая + аддитивный грейн + виньетка. Нет halation/
  bloom/LUT, грейн не взвешен по яркости. План: bloom по near-white туши,
  грейн сильнее в средних тонах, toe/shoulder.
- [LOW] Стабильность туши по масштабу: feTurbulence в user-space → на ECU
  boil растягивается. План: рендер частей в супер-сэмпле, baseFrequency ∝ 1/scale.
- [LOW] Кэш растеризованных частей (сейчас только сеты кэшируются) — перф.

## 2. Камера (src/timeline, src/parser, src/ast)

- [СДЕЛАНО] pitch + angle-пресеты + лерп + перенос ракурса через склейки.
- [HIGH] **Рыбий глаз (fisheye)** — фирменный приём на ECU/пробитии 4-й стены
  (CAMERA.md). План: пост-фильтр радиальной barrel-дисторсии кадра (сила k,
  центр в фокусе).
- [MED] **Focal/FOV и distance** отдельно от zoom (сейчас zoom — плоский
  масштаб). Нужны для dolly-zoom (эффект Вертиго) и силы перспективы.
- [MED] Разнообразие тряски (handheld/удар/гул) — сейчас один синус-профиль.
- [MED] Rack focus / глубина резкости — нет примитива блюра (см. §1 атмосфера).
- [LOW] Именованные движения dolly/truck/crane; dutch/pitch как keyframable
  кривые, а не мгновенный холд.

## 3. Риг / анимация (src/skeleton, src/procedural, rig.json)

Корень многих проблем: рабочий путь Фримена — skeleton+renderer, а лучшие
фичи (staggered-интерполяция, пружина ткани, антиципация-присед) живут в
НЕиспользуемом procedural-движке. Свести их в один путь.

- [HIGH] **On-twos/boil/grain выключены по умолчанию** (`on_twos:0`,
  `line_boil:0`, `film_grain:0`) — базовый рендер «гладкий», не Фримен.
  План: для рига дефолт on-twos=2 и малые boil/grain (или включать в шаблоне
  сцены). (Осознанно НЕ трогал глобальные дефолты в этом проходе, чтобы не
  сломать существующие сцены — вынести в пресет-конфиг Фримена.)
- [HIGH] **Нет overlapping action**: `interpolate_bone` применяет один
  t_smooth ко всем костям; стаггер (`apply_spring_lag`) только для head/
  upper_arm/forearm. Кисти/ноги/бровь/шляпа/трость приезжают в позу разом —
  главный «вырезанный» тэлл. План: per-bone задержка (конечности отстают от
  торса) или расширить пружину на кисти/ноги/шляпу.
- [HIGH] **Нет симуляции ткани** плаща/подола/фалд — подол вшит в жёсткий
  torso.svg. Пружинный `clothing_swing` есть только в procedural. План:
  добавить кости подола/фалд + follow-through пружина от скорости торса.
- [MED] Две кривые изинга; антиципация одной формы на всё тело; `apply_
  anticipation`/`overshoot` — мёртвый код. План: per-pose easing + per-bone
  масштаб антиципации; подключить/удалить мёртвые хелперы.
- [СДЕЛАНО] **Саккады/дарты глаз** — глаз держит точку фиксации ~0.95–1.5с и
  МГНОВЕННО прыгает на новую (саккада ~1 кадр), баес к центру (gx·|gx|): почти
  все дарты мелкие, изредка заметный взгляд в сторону; общий хэш «gaze» → оба
  глаза смотрят вместе (не косят), дарт подавлен на закрытых веках. Детермини-
  рованно (time+hash) — on-twos пиксель-идентичность держится (проверено A/B:
  held-пара OFF/ON = grain-only). Амплитуда через `ANIMDSL_SACCADE_AMP` (по
  умолч. 1.0). Разблокирует side-glance/skeptic-look из листа поз.
- [MED] Offset-интерполяция линейная (голова/шляпа/бровь/рот едут по прямой
  хорде, не по дуге). План: дуга/изинг на offset или крупные перемещения
  через поворот родителя.
- [СДЕЛАНО частично] Squash&stretch: было только на торсе от скорости
  трансляции (стоящий говорун не сжимался вовсе). Добавлен **акцент-squash**:
  на ударном слоге (speech_accent, импульс 1→0 за ~0.25с) яйцо-маска и корпус
  СЖИМАЮТСЯ по Y + разбегаются по X и отпускают со спадом — вес удара
  («тело бьёт на акценте»). Применён симметрично к current+prev тику (без
  фантом-смаза), on-twos держится. Осталось: импульс-присед на посадке прыжка
  (landing squash) — редко нужен для говорящих планов.
- [MED] Cel-swap и z_order прыгают на середине интерполяции (t≥0.5) — для
  0.35с смены руки заметный поп. План: свап на старте позы или на пике
  антиципации.
- [MED] Виземы неполны: ~6 форм рта, `mouth_wide` переиспользован для angry и
  визимы A; нет L/TH-языка, нет отдельного EE vs AA. Нет коартикуляции/холда.
- [LOW] **Позы-добор** из листа 24 эмоций: wink, meditate, tired/bored,
  side-glance/skeptic-look, peace (V), curl-up, balance, peek; walk/run-키.
  (side-glance/skeptic разблокированы — дарты глаз добавлены, см. §3.)

Хорошее (не регрессить): lerp_angle shortest-path; smear/multiples;
held-frame пружина (on-twos пиксель-идентичны); pose_variance;
спайн-bend (кривая line-of-action); богатый idle (дыхание/sway/вес/моргание).

## 4. DSL / сцена / локации / завод (src/parser, src/scene, tools)

- [HIGH] **Слои глубины/параллакс/передний план** — `layer` это просто
  z-order int; сет — один плоский SVG, трекает камеру 1:1. Нет мультиплан-
  фона, параллакса по глубине, плана-окклюдера, depth→scale. Главный
  сценический пробел.
- [HIGH] **Свет как реплика сцены** — нет light/shadow/grade-стейтмента
  (см. §1 свет).
- [HIGH] **Текст на экране как примитив DSL** + встроенные шрифты. Титры —
  внешние SVG; движок грузит только системные шрифты (`Arial Black` нет на
  ubuntu-latest → тихий фолбэк). **Нет трафаретного/тюремного шрифта** —
  надписи на стенах (`prison-cell.svg` использует DejaVu Sans, не трафарет).
  План: примитив `title/caption` + бандл шрифтов (в т.ч. stencil для тюрьмы),
  кинетическая типографика. (Требование пользователя: надписи в тюрьме —
  аутентичным казённым/трафаретным шрифтом; см. CAMERA.md чек-лист.)
- [MED-HIGH] **Атмосфера/частицы как сцена-реплика** — только глобальный
  `snow`. Нет дождя/дыма/пыли/искр/тумана, эмиттеров, ветра, spot-color частиц.
- [MED] Движение пропов/персонажей — только линейный отрезок + 4 изинга; нет
  path/безье, мультикейфреймов пропов, поз пропов.
- [MED] Слабый ensemble-стейджинг: нет look-at/eyeline, «facing ENT»,
  формаций, автоспейсинга.
- [MED] Нет звук/музыка-реплики в DSL (VO/SFX вне языка, синк через `timing`).
- [MED] Нет control-flow/переиспользования: нет переменных (кроме prop-let),
  циклов, шаблонов шотов/сцен — клоны копируются руками.
- [MED] **Завод/качество**: каждый шаг «мягко падает» в тихое видео → нет
  QC-гейта (пропавший голос/десинк проходят CI молча). Видео коммитятся в git
  с лимитом 100МБ — не масштаб на сезон. План: гейт качества (фейл при
  отсутствии VO/десинке), golden-frame diff, EBU R128, артефакты вне git.
- [MED] Единый голос, SFX процедурные — нет пути на мультидиалог/эмбиенс.
- [MED] **VO-схема + библиотека сценариев/реплик** — VO это markdown-таблица
  таймкодов, без валидации; регистр Фримена только в прозе-рубриках. План:
  схема VO + переиспользуемый банк реплик + линтер по charisma/similarity-
  рубрикам. (Ближайшая задача — сценарий хука в DIRECTING.md-регистре.)

---

## Порядок работ (следующие рубежи)

1. **Свет и тень** (cel-shade + rim + drop-shadow по позе) — самый большой
   скачок «киношности» картинки.
2. **Слои глубины/параллакс + передний план** — объём сцены.
3. **Типографика + бандл шрифтов (в т.ч. тюремный трафарет)** — бренд/титры/
   надписи на стенах аутентичны.
4. **Overlapping action + ткань плаща** — рисунок перестаёт быть вырезкой.
5. **Fisheye + focal/FOV (dolly-zoom)** — арсенал объектива под манифесты.
6. **Атмосфера-частицы по сценам** (дым/пыль/дождь/искры).
7. **Завод: QC-гейт, шрифты, артефакты вне git** — масштаб на сезон.
8. **Саккады глаз + добор поз (wink/meditate/tired/…)** — живое лицо, полный
   актёрский диапазон.
