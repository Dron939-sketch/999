meta:
  tool: docker
  toolchain: rust:nightly-bookworm

build:
  dockerfile: Dockerfile
  skip: false

run:
  persistenceMount: /data
  command: animdsl
  args: render input.anim -o /data/output.mp4
